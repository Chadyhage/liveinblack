import { useEffect, useMemo, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { useAuth } from '../context/AuthContext'

const STATUS = ['all','public','draft','hidden','suspended','pending_review']
export default function OrganizerAdminPage(){
  const {user}=useAuth(); const navigate=useNavigate()
  const [profiles,setProfiles]=useState([]); const [reports,setReports]=useState([]); const [filter,setFilter]=useState('all'); const [busy,setBusy]=useState('')
  useEffect(()=>{let a=()=>{},b=()=>{};import('../utils/firestore-sync').then(({listenAllOrganizerProfiles,listenOrganizerReports})=>{a=listenAllOrganizerProfiles(setProfiles);b=listenOrganizerReports(setReports)}).catch(()=>{});return()=>{a();b()}},[])
  const visible=useMemo(()=>filter==='all'?profiles:profiles.filter(p=>p.status===filter),[profiles,filter])
  async function update(profile,patch){setBusy(profile.id);const {syncDocAwaitable}=await import('../utils/firestore-sync');await syncDocAwaitable(`organizer_profiles/${profile.id}`,{...patch,updatedAt:Date.now(),lastAdminAction:{by:user.uid||user.id,at:Date.now(),patch:Object.keys(patch)}});setBusy('')}
  async function resolve(report,status){const {syncDocAwaitable}=await import('../utils/firestore-sync');await syncDocAwaitable(`organizer_reports/${report.id}`,{status,updatedAt:Date.now(),resolvedBy:user.uid||user.id})}
  return <main style={{minHeight:'100vh',background:'#04040b',color:'#fff',padding:'28px 22px 90px',fontFamily:'Inter,sans-serif'}}><div style={{maxWidth:1200,margin:'0 auto'}}><button onClick={()=>navigate('/agent')} style={{background:'none',border:0,color:'rgba(255,255,255,.45)',cursor:'pointer'}}>← Retour à l’admin</button><h1 style={{font:'56px Bebas Neue',margin:'18px 0 5px'}}>Pages organisateurs</h1><p style={{color:'rgba(255,255,255,.45)'}}>Vérification, visibilité, médias et signalements.</p><div style={{display:'flex',gap:7,flexWrap:'wrap',margin:'24px 0'}}>{STATUS.map(status=><button key={status} onClick={()=>setFilter(status)} style={{padding:'9px 12px',border:`1px solid ${filter===status?'#4ee8c8':'rgba(255,255,255,.12)'}`,background:filter===status?'rgba(78,232,200,.08)':'none',color:filter===status?'#4ee8c8':'rgba(255,255,255,.55)',font:'9px DM Mono',textTransform:'uppercase',cursor:'pointer'}}>{status==='all'?'Toutes':status} · {status==='all'?profiles.length:profiles.filter(p=>p.status===status).length}</button>)}</div><div style={{display:'grid',gap:10}}>{visible.map(profile=><section key={profile.id} style={{border:'1px solid rgba(255,255,255,.1)',background:'rgba(8,10,18,.62)',padding:16,display:'grid',gridTemplateColumns:'minmax(220px,1fr) auto',gap:16,alignItems:'center'}}><div><div style={{display:'flex',gap:9,alignItems:'center'}}><h2 style={{font:'26px Bebas Neue',margin:0}}>{profile.publicName}</h2>{profile.isVerified&&<span style={{color:'#4ee8c8',fontSize:10}}>● Vérifié</span>}</div><p style={{font:'9px DM Mono',color:'rgba(255,255,255,.4)'}}>{profile.slug} · {profile.status} · {profile.followersCount||0} abonnés · {(profile.media||[]).length} médias</p></div><div style={{display:'flex',gap:6,flexWrap:'wrap',justifyContent:'flex-end'}}><button className="btn-outline" onClick={()=>update(profile,{isVerified:!profile.isVerified})}>{profile.isVerified?'Retirer le badge':'Vérifier'}</button><select value={profile.status} onChange={e=>update(profile,{status:e.target.value,isPublic:e.target.value==='public'})} disabled={busy===profile.id} style={{background:'#0b0d14',color:'#fff',border:'1px solid rgba(255,255,255,.18)',padding:'8px',font:'9px DM Mono'}}>{STATUS.slice(1).map(s=><option key={s}>{s}</option>)}</select></div>{(profile.media||[]).length>0&&<div style={{gridColumn:'1 / -1',display:'flex',gap:8,overflowX:'auto',paddingTop:10,borderTop:'1px solid rgba(255,255,255,.07)'}}>{profile.media.map(media=><div key={media.id} style={{width:120,flex:'0 0 auto'}}><div style={{height:72,background:'#111',overflow:'hidden'}}>{media.type==='video'?<video src={media.url} muted style={{width:'100%',height:'100%',objectFit:'cover'}}/>:<img src={media.url} alt="" style={{width:'100%',height:'100%',objectFit:'cover'}}/>}</div><button onClick={()=>update(profile,{media:profile.media.map(item=>item.id===media.id?{...item,visibility:item.visibility==='hidden'?'public':'hidden'}:item)})} style={{width:'100%',padding:6,background:'none',border:'1px solid rgba(255,255,255,.12)',color:media.visibility==='hidden'?'#4ee8c8':'#e05aaa',fontSize:9,cursor:'pointer'}}>{media.visibility==='hidden'?'Réafficher':'Masquer'}</button></div>)}</div>}</section>)}</div>
  <section style={{marginTop:44}}><h2 style={{font:'36px Bebas Neue'}}>Signalements</h2>{reports.length===0?<p style={{color:'rgba(255,255,255,.4)'}}>Aucun signalement organisateur.</p>:<div style={{display:'grid',gap:9}}>{reports.sort((a,b)=>(b.createdAt||0)-(a.createdAt||0)).map(report=><div key={report.id} style={{border:'1px solid rgba(224,90,170,.2)',padding:14,display:'flex',gap:14,alignItems:'center',flexWrap:'wrap'}}><div style={{flex:1,minWidth:220}}><b style={{fontSize:13}}>{report.reason}</b><p style={{fontSize:11,color:'rgba(255,255,255,.45)',margin:'4px 0 0'}}>Page {report.organizerProfileId} · {report.description||'Aucune précision'} · {report.status}</p></div><button className="btn-outline" onClick={()=>resolve(report,'in_review')}>Examiner</button><button className="btn-gold" onClick={()=>resolve(report,'resolved')}>Résoudre</button></div>)}</div>}</section></div></main>
}
